#!/bin/bash
if [ ! $# -eq 2 ]
then
	echo 'Usage: downsample-sox [bit-depth] [sample-rate]'
	exit 1
fi
destdir="${PWD##*/}"
mkdir "$destdir"
cp ./*.jpg "$destdir"/
parallel sox '{}' -b "$1" -r "$2" -S "$destdir/{}" ::: *.flac
